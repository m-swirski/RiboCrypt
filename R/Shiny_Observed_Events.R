# TODO, understand how we can pass events, right now cds() argument will make it crash

# cds_display_on_event <- function(input, cds) {
#   if (input$gene %in% c("", "NULL")) {
#     cds[0]
#   } else {
#     if (input$gene %in% names(cds())) {
#       cds[input$gene]
#     } else cds[0]
#   }
# }
