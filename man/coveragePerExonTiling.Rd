% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_helpers.R
\name{coveragePerExonTiling}
\alias{coveragePerExonTiling}
\title{Tile coverage while keeping exon identities}
\usage{
coveragePerExonTiling(grl, reads, ...)
}
\arguments{
\item{grl}{a `GRangesList` with one transcript or feature per element and
one or more exon-like ranges inside each element.}

\item{reads}{reads or coverage input accepted by
[ORFik::coveragePerTiling()].}

\item{...}{extra arguments passed to [ORFik::coveragePerTiling()].}
}
\value{
a `data.table` from [ORFik::coveragePerTiling()] with
  `transcript_id`, `transcript_index`, `exon_id`, `exon_rank`,
  `exon_width`, and `exon_position` columns.
}
\description{
`coveragePerTiling()` concatenates the ranges in each `GRangesList`
element. This helper keeps that efficient coverage call, then annotates each
tiled row with the source exon so exon-level transcript QC can be done
without a second coverage pass.
}
